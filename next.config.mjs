/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["vudka-img.s3.eu-north-1.amazonaws.com", "vudka-pictures.s3.eu-north-1.amazonaws.com"],
      },
};

export default nextConfig;
